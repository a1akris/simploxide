//! The crate is empty and reserved for future use. Please, use the `simploxide-client` directly
//! instead;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
